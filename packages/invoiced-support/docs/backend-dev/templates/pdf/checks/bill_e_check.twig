<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{ title }}</title>
    <style>
        *, *:before, *:after {
            -moz-box-sizing: border-box;
            -webkit-box-sizing: border-box;
            box-sizing: border-box;
        }
        html,body {
            text-align: left;
            font-family: Helvetica, sans-serif;
            font-size: 10pt;
            margin: 0;
            padding: 0;
            width: 100%;
        }
        .page {
            height: 9.51in;
        }
        div {
            display: inline-block;
            line-height: 0.2in;
            width: 100%;
        }
        .check-print {
            margin: 0;
            height: 3.17in;
            font-family: sans-serif;
            font-size: 0.2in;
            padding-top: 0.7in;
        }
        .check-date {
            text-indent: 7.25in;
        }
        .general {
            padding-top: 0.42in;
        }
        .check-to {
            width: 70%;
            padding-top: 0.05in;
            text-indent: 1.05in;
        }
        .check-amount {
            width: 29%;
            vertical-align: top;
            text-indent: 1in;
        }

        .check-amount-text {
            padding-left: 0.2in;
            padding-top: 0.1in;
            height: 0.32in;
            width: 90%;
        }

        .address {
            text-indent: 1.05in;
            font-size: 0.1in;
            line-height: 0.1in;
            height: 0.28in;
        }

        .check-memo {
            text-indent: 0.8in;
            padding-top: 0.55in;
        }

        .line-items {
            margin-top: 0.75in;
            width: 100%;
            padding-left: 0.23in;
            height: 3in;
        }

        .line-items table {
            width: 100%;
            border-collapse: collapse;
        }

        .line-items th {
            border-bottom: 1px solid black;
            border-top: 1px solid black;
        }

        .line-items th {
            padding: 0;
        }

        .line-items tfoot th {
            border-bottom: 0;
        }

        .check-numbers{
            text-align: center;
        }
    </style>
</head>
<body>
{% for check in checks %}
    <div class="page">
        {%  include '/pdf/checks/bill_check.twig' %}
        <div class="check-numbers">
            {{ "%06d"|format(check.check_number) }} {{ check.account_number }} {{ check.check_number }}
        </div>

        <div class="line-items">
            <table>
                <thead>
                <tr>
                    <th class="date">Date</th>
                    <th>Document</th>
                    <th class="money-column">Amount</th>
                </tr>
                </thead>
                <tbody>
                <!-- we limit rows to maximum of 10 items to fit the check -->
                {% for bill in check.bills|slice(0,10) %}
                    <tr class="line">
                        <td class="date">
                            {{ bill.date }}
                        </td>
                        <td>
                            <div class="title">{{bill.number}}</div>
                        </td>
                        <td class="money-column">
                            {{ bill.amount|number_format(2) }}
                        </td>
                    </tr>
                {% endfor %}
                {% if check.bills|length > 10 %}
                    <tr>
                        <td class="date" colspan="3">
                            More...
                        </td>
                    </tr>
                {% endif %}
                </tbody>
                <tfoot>
                <tr>
                    <th colspan="2"></th>
                    <th class="money-column">{{ check.amount|number_format(2) }}</th>
                </tr>
                </tfoot>
            </table>
        </div>

        {%  include '/pdf/checks/bill_check.twig' %}
    </div>
{% endfor %}
</body>
</html>