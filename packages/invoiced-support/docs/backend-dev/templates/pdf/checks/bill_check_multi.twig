<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
    <style>
        *, *:before, *:after {
            -moz-box-sizing: border-box;
            -webkit-box-sizing: border-box;
            box-sizing: border-box;
        }
        html,body {
            text-align: left;
            font-family: Helvetica, sans-serif;
            font-size: 10pt;
            margin: 0;
            padding: 0;
            width: 100%;
        }
        .page {
            height: 9.51in;
        }
        div {
            display: inline-block;
            line-height: 0.2in;
            width: 100%;
        }
        .check-print {
            margin: 0;
            height: 3.53in;
            font-family: sans-serif;
            font-size: 0.2in;
            padding-top: 0.7in;
        }
        .check-date {
            text-indent: 7.1in;
        }
        .general {
            padding-top: 0.6in;
        }
        .check-to {
            width: 70%;
            text-indent: 1.05in;
        }
        .check-amount {
            width: 29%;
            padding-top: 0.02in;
            vertical-align: top;
            text-indent: 0.9in;
        }

        .check-amount-text {
            padding-left: 0.2in;
            height: 0.32in;
            width: 90%;
        }

        .address {
            text-indent: 1.05in;
            font-size: 0.1in;
            line-height: 0.1in;
            height: 0.28in;
        }

        .check-memo {
            text-indent: 0.8in;
            padding-top: 0.4in;
        }
    </style>
</head>
<body>
<div class="page">
    {% for check in checks %}
        {%  include '/pdf/checks/bill_check.twig' %}
        {% if loop.index % 3 == 0 %}
            </div>
            <div class="page">
        {% endif %}
    {% endfor %}
</div>
</body>
</html>