{% form_theme form with '@EasyAdmin/crud/form_theme.html.twig' %}
{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}Integration Logs{% endblock %}

{% block body_class 'ea-edit' %}

{% block page_actions %}
    {{ parent() }}
    <button type="submit" class="btn btn-primary" form="searchForm">Search</button>
{% endblock %}

{% block main %}
    {{ form_start(form, {attr: {id: 'searchForm'}}) }}

    <p>Search for integration logs from a specific tenant. Integration logs are retained for 90 days. In order for integration logs to be captured for an account the appropriate <code>log_</code> feature flag must be enabled.</p>

    {% if error is defined %}
        <div class="alert alert-danger">{{ error }}</div>
    {% endif %}

    <div class="row pb-5">
        <div class="col-md-6 col-xxl-5">
            {{ form_widget(form) }}
        </div>
    </div>

    {{ form_end(form) }}
{% endblock %}